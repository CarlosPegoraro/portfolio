name: portfolio

services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5100:5173"
    environment:
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - ..:/web
      - bun_modules:/web/node_modules
volumes:
  bun_modules: